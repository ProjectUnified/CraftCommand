/**
 * Validation annotations for CraftCommand parameters.
 *
 * <p>Annotations like {@code @Required}, {@code @Min}, {@code @Max} can be placed
 * on command parameters to enforce constraints at runtime.
 */
package io.github.projectunified.craftcommand.validation.annotation;
